{% extends 'base.html.twig' %}

{% block title %}Hello AddAthleteController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

    <div class="example-wrapper">
        <h1>Hello {{ form_title }}! ✅</h1>
        {{ form_start(form_athlete) }}
        {{ form_row(form_athlete.Nom) }}
        {{ form_row(form_athlete.Prenom) }}
        {{ form_row(form_athlete.Pays) }}
        <input
                id="sport"
                type="number"
                class="form-control"
                name="sport"
        />
        <button type="submit" class="btn">Register nouveau sport</button>
        {{ form_end(form_athlete) }}
    </div>
{% endblock %}
